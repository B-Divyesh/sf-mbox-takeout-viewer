# Streamed, in-browser viewer/search/extractor for multi-GB Gmail Takeout .mbox files

Live: https://mbox-takeout-viewer.sociobot.in — built by the Param Factory (`pwa-offline`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
